# Вердикт: docs/query/api/resource.md

**Статус**: PASS (после 2 исправлений)

## Проверки

| # | Критерий | Результат |
|---|----------|-----------|
| 1 | Все опции из usage/resource.md | OK — 6/6 (`queryFn`, `key`, `cacheRetentionTime`, `serializeArgs`, `onCacheEntryAdded`, `onQueryStarted`) |
| 2 | Все методы из usage/resource.md | OK — 7/7 (`useResource`, `trigger`, `refresh`, `getEntry`, `getEntry$`, `createAgent`, `link`) |
| 3 | Формат таблиц совпадает с api/README.md | OK после фикса — таблица методов приведена к 4-колоночному формату (Метод / Параметры / Возвращаемое значение / Описание) |
| 4 | Только reference-style ссылки | OK — все ссылки `[text][ref]`, определения в конце файла |
| 5 | Нет дублирования с usage/resource.md | OK — таблицы опций/методов без описательного контента, примеры кода минимальны |
| 6 | Русский язык, естественные формулировки | OK после фикса — текст ссылки `usage/resource.md` заменён на «руководство по использованию» |
| 7 | Нет code-only информации (правило #11) | OK — все опции и методы присутствуют в usage/resource.md |

## Исправления

1. **Текст ссылки**: `«в [usage/resource.md][usage]»` → `«см. [руководство по использованию][usage]»` — сырой путь заменён на естественный русский текст.
2. **Таблица методов**: 3 колонки → 4 колонки. Параметры вынесены из имени метода в отдельную колонку «Параметры», как в api/README.md.
