Замечания:

1) createSignalHooks вместо createHooks
2) в createSignalHooks нужно вызывать createState, а не придумывать велосипед
3) Каждая фаза - законченный набор работ, ошибки компиляции не допустимы
