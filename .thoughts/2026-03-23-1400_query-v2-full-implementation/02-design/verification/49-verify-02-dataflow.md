# Verification: 02-dataflow.md

## Issues Checked

| # | Issue | Relevant | Status | Evidence |
|---|-------|----------|--------|----------|
| 2 | Commands removed completely | Yes | PASS | Zero mentions of "Command", "createCommand", or any command-related terminology in the entire file. Grep across all 700 lines confirms clean. |
| 8 | Batcher.run() optional for single changes | Yes | PASS | §1.1 Note on line 33: "Batcher.run() wraps multi-signal transitions (optional for single-signal changes — see §7.3)". §6.3 provides dedicated explanation: single signal change propagates immediately, Batcher.run() is for grouping multiple writes. |
| 17 | Operations/OperationV2 completely removed | Yes | PASS | Zero mentions of "Operation", "OperationV2", "useOperationV2" anywhere in the file. All scenarios use ResourceV2/ResourceV2Agent/ResourceV2CacheEntry exclusively. |
| 21 | machine$ is a signal, not a method | Yes | PASS | §6.1 shows `current.machine$()` and `previous?.machine$()` with parentheses (signal read) and explicit comment: "machine$ property (reactive alias for inherited state$())". No instance of machine$ used as a method call. |
| 26 | Resource.invalidate delegates to RCE.invalidate | Yes | PASS | §1.4 line 95–96: `App->>Res: invalidate(args: TArgs): void` → `Res->>RCE: invalidate(): void [via CacheMap.get(args: TArgs)]`. §5.3 flowchart confirms: `ResourceV2.invalidate(args: TArgs)` → `CacheMap.get(args: TArgs)` → `RCE.invalidate(): void`. |
| 31 | Remove refreshError, onRefreshError, notifyRefreshError | Yes | PASS | Zero matches for "refreshError", "onRefreshError", "notifyRefreshError" in the file. §1.4 error case correctly shows ADR-2 behavior (stay in Success, preserve stale data) with no refreshError concept. |
| 32 | Dataflow diagrams show what and when is returned | Yes | PASS | Every sequence diagram shows `Agent-->>App: state$: {status:..., data:..., isLoading:...}` at appropriate transition points. §1.1: pending→success with data values. §1.2: SWR shows stale data during pending, fresh data on success. §1.4: refreshing with stale data, then success/error outcomes. §1.6: error→retry showing data:null then data:TData. |
| 33 | "1.3 Cache Hit" simplified | Yes | PASS | §1.3 contains only 3 meaningful interactions: start(args) → compareArgs → "No action — args unchanged" → state$ unchanged. Minimal, no extraneous entities. |
| 34 | "1.7 GC Lifecycle" — abort patches justified or removed | Yes | PASS | §1.7 GC cleanup decisions explicitly addresses this: "**Abort patches — NO**: When refcount reaches 0 and the GC timer fires, no subscribers exist. IPatchHandle references are obtained through the entry and held by consumers. If all consumers have unmounted (refcount=0), no one holds active patch handles — pending patches are impossible. No abort needed." Clear justification provided. |
| 35 | "1.7 GC Lifecycle" — abortController.abort() presence | Yes | PASS | §1.7 GC cleanup decisions: "**Abort inflight fetch — YES**: ...Calling `_abortController.abort()` prevents wasted network requests on an entry being destroyed." Diagram Note on CE: "abort inflight fetch (if any) → fire onClean$". Ref to ADR-17. |
| 36 | "3.4 ReactHooksPlugin Lifecycle" — graph connectivity | Yes | PASS | §3.4 uses 3 annotated phases within one sequence diagram. All phases share the "App" participant as connecting node. Phase 1 (registration): App→API→RHP. Phase 2 (augmentation): App→API→RHP→API→App. Phase 3 (hook usage): App→Hook→App. No disconnected subgraphs — the "App" participant links all phases. |
| 37 | TArgs typed everywhere `<TArgs, TData>` | Yes | PASS | Comprehensive audit: every machine type `MachinePending<TArgs, TData>`, `MachineSuccess<TArgs, TData>`, `MachineError<TArgs, TData>`, `MachineRefreshing<TArgs, TData>`. All function calls typed: `queryFn(args: TArgs, {abortSignal: AbortSignal}): Promise<TData>`, `start(args: TArgs): void`, `_getEntry(args: TArgs)`, etc. §5.1–5.5 flowcharts: `TMachineInstance<TArgs, TData>`, `CacheEntry.set(machine: TMachineInstance<TArgs, TData>)`. No bare `<TData>` without `<TArgs, TData>`. |
| 39 | Disconnected node in §5.2 | Yes | PASS | §5.2 Read Path: A→B→C→{D,E}, E→D, D→F, F→G, G→{H, H2}. All nodes connected. H and H2 are distinct nodes (`ResourceV2Agent.state$` and `useSignal React snapshot`). No disconnected or duplicate nodes. |
| 42 | All machines accept `<TArgs, TData>` | Yes | PASS | Every formal machine reference uses `<TArgs, TData>`: `MachinePending<TArgs, TData>` (§1.1, §1.2, §1.5, §1.6, §5.1), `MachineSuccess<TArgs, TData>` (§1.1, §1.2, §1.4, §1.5, §1.6, §2.1, §5.3), `MachineRefreshing<TArgs, TData>` (§1.4, §1.5), `MachineError<TArgs, TData>` (§1.6, §5.1). Informal mentions in Notes (line 77 "MachineSuccess with fresh data", line 129 "MachinePending, inflight request active") are descriptive prose in diagram annotations — acceptable. |
| 43 | All machines contain args: TArgs | Yes | PASS | §4.1 State data availability table explicitly shows `args` column: idle=null, pending=TArgs, success=TArgs, error=TArgs, refreshing=TArgs. All non-idle states carry `args: TArgs`. Idle has `args: null` (field present, no value before start). |
| 44 | All function calls show argument types | Yes | PASS | Comprehensive typed signatures throughout: `start(args: TArgs): void`, `_getEntry(args: TArgs)`, `query(): Promise<TData>`, `queryFn(args: TArgs, {abortSignal: AbortSignal}): Promise<TData>`, `set(MachinePending<TArgs, TData>)`, `invalidate(args: TArgs): void`, `compareArgs(a: TArgs, b: TArgs): boolean`, `CacheMap.get(args: TArgs)`, `CacheMap.delete(args: TArgs): boolean`, `createPatch(patchFn: (draft: TData) => void): IPatchHandle | null`, `Patcher.resolvePatches(originalData: TData, patches: TPatch[]): IPatchResolution<TData>`, etc. One minor: `LifecycleHooks.fireCacheEntryRemoved` in §5.4 lacks argument types — a flowchart label for an internal side effect; non-critical. |
| 45 | §1.6 shows RCE data return on retry | Yes | PASS | §1.6 diagram shows complete return chain: `QFn-->>RCE: data: TData` → `RCE->>RCE: set(MachineSuccess<TArgs, TData>)` → `RCE-->>Agent: state$ signal fires (reactive subscription)` → `Agent->>Agent: derive flat state from MachineSuccess<TArgs, TData>` → `Agent-->>App: state$: {status:"success", data: TData, isLoading:false}`. |
| 48 | initialSnapshot save/consume-delete/delete | Yes | PASS | §2.1 documents exact 3-phase lifecycle: (1) `createApi({initialSnapshot})` → "_savedSnapshot = initialSnapshot" (save). (2) `api.createResourceV2()` → consumes snapshot slice, hydrates entries, "Delete slice from _savedSnapshot" (consume+delete). (3) `api.resetAll()` → "_savedSnapshot = null (saved snapshot deleted entirely)" (delete). §3.3 confirms: "Snapshot is saved, NOT hydrated yet. Per-resource consumption happens in createResourceV2." |
| 49 | ADR-12 snapshot contamination — no "if exists"/"skip" in hydration flows | Yes | PASS | Zero matches for "already exists", "if exists", "skip hydrat", "conditional hydrat" in the file. §2.1 hydration alt-branch is `alt _savedSnapshot has slice for resource.key` — checks snapshot content, NOT whether a resource exists. §3.3 note: "Snapshot is saved, NOT hydrated yet" — no global hydration at API creation. No decision diamond checking "does resource exist?". |
| R-2 | §5.2 Read Path node connectivity fixed | Yes | PASS | §5.2 flowchart: A→B→C, C→D (Yes), C→E (No), E→D (doInitiate=true), D→F, F→G, G→H, G→H2. All nodes fully connected. G has incoming edge from F. H and H2 are distinct nodes with separate labels. No orphaned or duplicate nodes. |

## Additional Findings

1. **Minor — §5.4 `LifecycleHooks.fireCacheEntryRemoved`**: This flowchart label doesn't show argument types. Since this is an internal framework event label in a high-level GC flowchart (not a formal API call), severity is negligible. Does not violate the spirit of #44 (which targets user-facing and cross-component API calls).

2. **Informal machine mentions in Mermaid Notes**: Lines 77 ("MachineSuccess with fresh data") and 129 ("MachinePending, inflight request active") omit `<TArgs, TData>` generics. These are descriptive annotations within diagrams — not code or type references. All formal `set()` calls and type expressions consistently use `<TArgs, TData>`. Acceptable per #42 intent.

## Verdict: PASS
