# Cross-File Consistency: 03-model.md ↔ 02-dataflow.md

**Date**: 2026-03-25
**Direction**: model → dataflow
**Primary**: `02-design/03-model.md`
**Comparison**: `02-design/02-dataflow.md`

## Results Summary

| Total | PASS | FAIL | WARN |
|-------|------|------|------|
| 25    | 23   | 0    | 2    |

## Cross-Check Items

| # | What (model) | Where (dataflow) | Status | Details |
|---|-------------|-------------------|--------|---------|
| 1 | `TMachineStatus` union: idle, pending, success, error, refreshing | §4.1 state diagram, §1.x sequence diagrams | PASS | All five statuses present in state diagram. Sequences use `MachinePending`, `MachineSuccess`, `MachineError`, `MachineRefreshing` consistently. Idle shown as initial state. |
| 2 | `TMachineState` field signatures: `TPendingState<TArgs>` has `data: null`, `TSuccessState<TArgs,TData>` has `data: TData`, patchState etc. | §4.1 "State data availability" table | PASS | Table matches model exactly: idle=null/null, pending=null/null, success=TData/null, error=null/unknown, refreshing=TData(stale)/null. Patches possible column matches model's patchState presence. |
| 3 | `TMachineInstance<TArgs, TData>` = union of 5 Machine classes | §1.x sequence diagrams use machine instances as states | PASS | Diagrams annotate states as `MachinePending<TArgs, TData>`, `MachineSuccess<TArgs, TData>`, `MachineRefreshing<TArgs, TData>`, `MachineError<TArgs, TData>`. Generics consistently `<TArgs, TData>`. |
| 4 | `Machine.fromSnapshot<TArgs, TData>(state)` static factory | §2.1 Hydrate diagram: `Machine.fromSnapshot<TArgs, TData>(slice)` | PASS | Dataflow §2.1 uses `Machine.fromSnapshot<TArgs, TData>(slice)` in the hydration loop — matches model §3 `IMachineStatic.fromSnapshot<TArgs, TData>(state: TMachineState<TArgs, TData>)`. |
| 5 | `ICacheEntry<TState>`: `state$()`, `peek()`, `set()`, `complete()`, `onClean$` | §1.7 GC: `complete(): void`, `onClean$`; §5.1 write path: `CacheEntry.set(machine)`, §6.1 signal chain | PASS | `set()` used in write path §5.1. `complete()` + `onClean$` used in GC §1.7 and §5.4. `state$()` referenced in §6.1 as "Signal.state<TState> write (inherited)". All match. |
| 6 | `ICacheMap<TArgs, TEntry>`: `get(args)`, `getOrCreate(args)`, `delete(args)` | §1.1: `_getEntry(args)` → `CacheMap.getOrCreate(args)`; §1.4: `CacheMap.get(args)`; §1.7 GC: `_cache.delete(args)` | PASS | `getOrCreate` used in initial fetch (§1.1 note). `get` used in invalidation path §1.4 and read path §5.2. `delete` used in GC §1.7 and §5.4. All signatures match model's `ICacheMap` interface. |
| 7 | `TCacheMapFactory<TArgs, TEntry> = (args: TArgs) => TEntry` | §1.1 note: "\_getEntry is a factory callback provided by ResourceV2 at agent creation. Internally calls CacheMap.getOrCreate(args: TArgs)" | PASS | Dataflow correctly describes factory indirection. Agent calls `_getEntry(args: TArgs)` which resolves to `CacheMap.getOrCreate`. Factory is not called directly in diagrams — consistent with model §6.4 where factory is internal to CacheMap. |
| 8 | `IResourceV2CacheEntry`: `machine$`, `isMyArgs()`, `createPatch()`, `invalidate()`, `query()` | §1.x: `query()`, `invalidate()`; §5.5: `createPatch()`; §6.2: `isMyArgs(args)` | PASS | `query()` used in §1.1, §1.2, §1.4, §1.5, §1.6. `invalidate()` in §1.4, §5.3. `createPatch()` in §5.5. `isMyArgs(args)` in §6.2. `machine$()` in §6.1 signal chain. All match model §7.3 interface. |
| 9 | `ResourceV2CacheEntry` constructor: `args`, `queryFn`, `compareArgs`, `cacheLifetime` | §1.1 note mentions `_getEntry` factory. Not detailed in dataflow. | PASS | Dataflow abstracts construction behind factory callback (consistent with model §6.4). Diagram shows `RCE` created via `_getEntry(args)` — does not enumerate constructor params. This is acceptable: constructor internals are model-level, not dataflow-level. |
| 10 | `TQueryFn<TArgs, TData> = (args: TArgs, tools: {abortSignal}) => Promise<TData>` | §1.1: `queryFn(args: TArgs, {abortSignal: AbortSignal}): Promise<TData>` | PASS | All queryFn invocations show `(args: TArgs, {abortSignal: AbortSignal}): Promise<TData>`. §1.4, §1.5, §1.6, §5.1, §5.3 all consistent. |
| 11 | `IResourceV2Agent<TArgs, TData>`: `state$`, `start(args)`, `compareArgs(a, b)` | §1.1: `Agent.start(args: TArgs): void`; §1.3: `compareArgs(a, b): boolean → true`; §6.1: `ResourceV2Agent.state$` | PASS | `start(args: TArgs): void` in §1.1, §1.2, §1.6. `compareArgs` in §1.3 cache hit scenario. `state$` in §6.1 reactive chain and all diagrams' `Agent-->>App: state$` messages. |
| 12 | Agent `_tracking$`: `{previous, current}` signal | §1.1: "update _tracking$ (current ← entry)"; §1.2: "previous ← current", "previous ← null" | PASS | §1.2 SWR shows previous/current swap explicitly: `previous ← current (old entry)`, `current ← entry`. After resolution: `previous ← null`. Matches model §8.1 `_tracking$: SignalFn<{previous: RCE | null, current: RCE | null}>`. |
| 13 | Agent `_getEntry` callback: `(args: TArgs) => ResourceV2CacheEntry` | §1.1 note: "\_getEntry is a factory callback provided by ResourceV2 at agent creation" | PASS | Dataflow's note matches model §8.1 `_getEntry: (args: TArgs) => ResourceV2CacheEntry<TArgs, TData>`. Return type not explicit in dataflow note, but usage shows typed entries `RCE<TArgs,TData>`. |
| 14 | Agent `_lastArgs` for cache hit detection | §1.3: `compareArgs(a, b) → true` → no action | WARN | Dataflow §1.3 shows cache hit via `compareArgs(a, b): boolean → true` leading to "No action — args unchanged", but does NOT mention `_lastArgs` field explicitly. The model's `_lastArgs: TArgs | SKIP_TOKEN | null` drives this logic. Dataflow abstracts the mechanism — functionally correct but omits internal state tracking. **Minor: consider adding `_lastArgs` to the SWR/cache-hit note for clarity.** |
| 15 | `IPatchHandle`: `commit()` and `abort()` | §5.5 flowchart: `commit` and `abort` branches from "User action" | PASS | §5.5 shows `commit` → "Patch status: committed" and `abort` → "Patch status: aborted". Matches model §3 `IPatchHandle { commit(): void; abort(): void }`. |
| 16 | `Patcher.createPatch(patchFn, data)` returns `{patch: TPatch, data: TData}` | §5.5: `Patcher.createPatch(patchFn, data: TData): {patch: TPatch, data: TData}` | PASS | Exact match. Dataflow §5.5 box: `Patcher.createPatch(patchFn, data: TData): {patch: TPatch, data: TData}`. Model §4 signature identical. |
| 17 | `Patcher.resolvePatches(originalData, patches)` returns `IPatchResolution<TData>` | §5.5: `Patcher.resolvePatches(originalData: TData, patches: TPatch[]): IPatchResolution<TData>` | PASS | Dataflow §5.5 commit branch uses `Patcher.resolvePatches(originalData: TData, patches: TPatch[]): IPatchResolution<TData>`. Matches model §4. |
| 18 | `TPatchState<TData>` with `originalData`, `patches[]`, `isConsistencyViolation` | §5.5: consistency violation → `entry.invalidate()` | PASS | Dataflow §5.5 abort path: "applyPatches inverse succeeds?" → No → "Consistency violation entry.invalidate()". Model §2 `TPatchState<TData>` has `isConsistencyViolation: boolean`. Dataflow covers the behavioral consequence correctly. |
| 19 | `IPlugin.install(context: IPluginContext)` and `augmentResource(resource, options)` | §3.1: `install(context: IPluginContext): void`, `augmentResource<TArgs, TData>(resource, options)` | PASS | §3.1 diagram shows exact signatures: `install(context: IPluginContext): void` and `augmentResource<TArgs, TData>(resource: IResourceV2, options: IResourceV2Options)`. Matches model §11. |
| 20 | `IApi.resetAll()` behavior: delete snapshot + reset resources | §6.2: `_status$.set("idle")` → getEntry$ returns null | WARN | Model §12.1 specifies `resetAll()`: "abort patches → reset machine to idle → complete entries → clear cache" and `_savedSnapshot = null`. Dataflow §6.2 shows `_status$` set to "idle" and getEntry$ returning null. §2.1 mentions `resetAll()` → `_savedSnapshot = null`. **Dataflow does not show the full resetAll sequence (abort patches → reset → complete → clear → snapshot=null) as a dedicated diagram.** The pieces are scattered across §6.2, §2.1, and §5.4. Functionally consistent, but a dedicated resetAll sequence diagram would improve traceability. |
| 21 | `TOnCacheEntryAdded(args, tools: {$cacheDataLoaded, $cacheEntryRemoved})` | §5.4 GC trigger: "LifecycleHooks.fireCacheEntryRemoved"; §5.1 write path: "LifecycleHooks resolve promises" | PASS | Dataflow §5.4 shows `LifecycleHooks.fireCacheEntryRemoved` on GC completion — maps to `$cacheEntryRemoved` promise. §5.1 shows `LifecycleHooks resolve promises` on success — maps to `$cacheDataLoaded`. Both lifecycle tool promises reflected. |
| 22 | `TOnQueryStarted(args, tools: {$queryFulfilled, getCacheEntry})` | §5.1 write path: "LifecycleHooks resolve promises" (implicit) | PASS | `$queryFulfilled` resolves on queryFn success (§5.1 write path) or rejects on error. Not explicitly named in dataflow, but "LifecycleHooks resolve promises" in §5.1 flowchart covers it. `getCacheEntry` is a simple accessor — no dataflow behavior to diagram. |
| 23 | `TApiSnapshot` with `version`, `keyPrefix`, `resources` | §2.1: `getSnapshot(): TApiSnapshot`, hydrate validates "version + keyPrefix" | PASS | §2.1 Capture: `api.getSnapshot(): TApiSnapshot`. §2.1 Hydrate: "Validate version + keyPrefix (throw on mismatch)". §3.3 also shows validation. Matches model §10. |
| 24 | `Batcher.run()` for multi-signal changes | §1.1 note: "Batcher.run() wraps multi-signal transitions"; §5.1: "Batcher.run groups multiple writes"; §6.3: full semantics | PASS | Dataflow §6.3 explicitly documents Batcher.run() is optional for single changes and needed for multi-signal atomicity. §5.1 write path shows `Batcher.run` between machine creation and signal update. Matches model's implicit batching pattern. |
| 25 | `share({resetOnRefCountZero})` GC + `cacheLifetime` config table | §1.7 GC sequence and cacheLifetime behavior table | PASS | §1.7 has complete sequence diagram + configuration table (false/0/>0 values). `complete()`, `onClean$`, `_cache.delete(args)` all present. Cleanup decisions (abort inflight YES, abort patches NO) explicitly documented. Matches model's CacheEntry complete() / onClean$ contract. |

## Summary

**Overall consistency: HIGH**. All types, interfaces, and signatures from the model are reflected accurately in the dataflow diagrams. Two minor warnings relate to completeness of internal state tracking details in the dataflow (not errors):

- **WARN #14**: Agent `_lastArgs` field not explicitly shown in dataflow cache-hit scenario — behavior is correct, internal mechanism abstracted away.
- **WARN #20**: `resetAll()` full sequence not consolidated in a single dataflow diagram — pieces are distributed across §6.2, §2.1, §5.4. Consider adding a dedicated resetAll sequence.

No contradictions, type mismatches, or signature inconsistencies found between model and dataflow.
