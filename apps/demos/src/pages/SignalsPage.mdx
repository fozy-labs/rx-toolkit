import { Tab } from "@heroui/react";
import { LiveExample, QueryTabs } from "../components";
import { Signals } from "../examples";

# Сигналы

## Примеры

<QueryTabs>
    <Tab key="store" title="Счетчик">
        <LiveExample
            title="Реактивный счетчик"
            initialCode={Signals.examples.counterStore}
        />
    </Tab>
    <Tab key="local" title="LocalSignal">
        <LiveExample
            title="Локальное хранилище"
            initialCode={Signals.examples.localSignal}
        />
    </Tab>
</QueryTabs>

